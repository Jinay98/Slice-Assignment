package com.slice.model.enums;

/**
 * Status lifecycle for an Item.
 *
 * INTERVIEW TIP: Replace with your domain-specific enum.
 * Examples:
 *  - NotificationStatus:  PENDING, SENT, FAILED, RETRYING
 *  - TransactionStatus:   INITIATED, PROCESSING, SUCCESS, FAILED, REVERSED
 *  - ParkingSlotStatus:   AVAILABLE, OCCUPIED, RESERVED, MAINTENANCE
 */
public enum ItemStatus {
    ACTIVE,
    INACTIVE,
    DELETED
}
